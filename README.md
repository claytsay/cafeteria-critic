# cafeteria-critic

`cafeteria-critic` is a Python library that aims to solve the problem of deciding what to eat.

The project contains code to scrape online cafeteria menus, process them into a LLM prompt,
and parse the results.
